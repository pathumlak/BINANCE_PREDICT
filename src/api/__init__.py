"""Phase 7 — Interactive dashboard.

Submodules:
  * app        – FastAPI app factory.
  * routes     – HTTP endpoints.
  * models     – Pydantic schemas.
  * inference  – Singleton service holding the trained fusion model,
                 pattern engine, CNN, and aligned per-bar data.
"""
