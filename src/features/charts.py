"""Deprecated stub.

The chart-image encoders moved to ``src.vision.encoders.*`` to live next
to the CNN backbone that consumes them. Update any imports:

    from src.vision.encoders.candlestick import render_candlestick
    from src.vision.encoders.gaf_mtf     import render_gaf_mtf
"""
